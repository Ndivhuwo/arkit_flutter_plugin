name: arkit_plugin_example
description: Insole Demo App.
publish_to: 'none'
version: 1.0.5+5

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_mlkit_object_detection: ^0.6.0
  path_provider: ^2.0.12
  #tflite: ^1.1.2
  camera: ^0.10.3
  image_picker: ^0.8.6
  arkit_plugin:
    path: ../
  torch_controller: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  #pedantic: ^1.11.1
  #google_mlkit_object_detection: ^0.6.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/ml/